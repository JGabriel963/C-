services:
  mysql:
    image: mysql:8.4
    container_name: cashflow-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: cashflow
      MYSQL_USER: cashflow
      MYSQL_PASSWORD: cashflow
    ports:
      - "3306:3306"
